package com.teukgeupjeonsa.backend.nutrition.matching;

public class FoodMatchEnums {
    public enum CandidateConfidence { HIGH, MEDIUM, LOW }
    public enum CandidateStatus { AUTO_APPROVED, NEEDS_REVIEW, REJECTED, APPROVED }
}
