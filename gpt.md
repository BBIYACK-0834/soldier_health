# 영양소 표시 기능 개선 기록

## 문제 원인

- 부대에서 제공되는 예정 식단을 사용자가 실제로 모두 먹은 것으로 계산했다.
- 공식 칼로리, 음식별 추정 칼로리, 탄단지 계산 기준이 홈과 식단 화면에서 서로 달랐다.
- 문서에 명시된 공식 칼로리 기반 보정 조건이 실제 코드에는 적용되지 않았다.
- 식품 매칭 결과를 무기한 캐시해 관리자가 DB 매칭을 수정해도 이전 결과가 남을 수 있었다.
- 목표 체중으로 기초대사량까지 계산해 감량·증량 목표가 클 때 목표 칼로리가 왜곡될 수 있었다.
- Gradle wrapper JAR와 ESLint flat config가 없어 백엔드 테스트와 프런트 린트가 실행되지 않았다.

## 적용한 변경

1. `user_meal_consumptions` 테이블을 추가하고 아침·점심·저녁별 실제 섭취 배율을 저장한다.
2. 부대 식단은 기본적으로 제공 예정 식단이며, 사용자가 `안 먹음/절반/기본/많이`를 선택해야 실제 섭취량에 반영한다.
3. 사용자가 직접 추가한 음식은 선택 즉시 실제 섭취량에 포함하고 부대 급식 배율의 영향을 받지 않는다.
4. 공식 칼로리가 있고 매칭률이 60% 이상이며 보정 비율이 0.6~1.6이면 음식별 칼로리와 탄단지를 같은 비율로 보정한다. 반올림 뒤 남는 오차는 마지막 매칭 항목에 반영해 항목 합계가 공식 칼로리와 정확히 일치하도록 한다.
5. 홈과 식단 상단은 API의 `totalCalories`, 즉 기록된 실제 섭취 칼로리를 공통 기준으로 사용한다.
6. 음식 매칭 무기한 캐시를 제거해 DB 및 수동 매칭 변경사항이 즉시 반영되도록 했다.
7. 기초대사량은 현재 체중과 프로필의 생년월일·성별을 기준으로 계산하고, 탄단지 목표만 설정된 목표 체중을 기준으로 계산한다. 기존 사용자에게 값이 없으면 남성 22세를 호환 기본값으로 사용한다.
8. Gradle wrapper JAR를 복구하고 ESLint 9용 `eslint.config.js`를 추가했다.
9. 실제 인증정보를 커밋하지 않도록 `backend/.env.example`에는 안전한 예시값만 기록했다.
10. 식품명 정규화가 `200ML`의 앞 두 자리까지 알레르기 번호로 오인하던 정규식을 수정했다.
11. 식품 엑셀의 설명 행과 헤더 행을 누락 데이터로 집계하던 파서의 시작 행 계산을 수정했다.
12. 민감한 인증 설정의 소스 코드 기본값을 제거해 환경변수가 빠진 상태로 안전하지 않게 실행되지 않도록 했다.

## API 변경

`PUT /api/users/me/meal-consumption/today`

```json
{
  "mealType": "lunch",
  "portionMultiplier": 1.0
}
```

허용 범위는 `0.0~2.0`이며 화면에서는 0, 0.5, 1.0, 1.5를 제공한다. 간식은 사용자가 추가한 음식 기록으로 계산한다.

## 환경변수 참고

- 실제 실행값은 `backend/.env`에 두고 Git에 커밋하지 않는다.
- README의 `source .env` 방식으로 실행하므로 `MEAL_COLLECTOR_AUTO_COLLECT_CRON`처럼 공백이 있는 값은 따옴표로 감싼다.
- `FOOD_IMPORT_FILE`은 백엔드 실행 디렉터리가 `backend/`인 경우 현재 설정인 `src/main/resources/food_data/food_data_7_13.xlsx`가 올바르다.
- 운영 환경에서는 예시 인증정보를 반드시 별도의 안전한 값으로 교체한다.

## 검증 결과

- 백엔드: Gradle 전체 테스트 30개 통과
- 프런트엔드: ESLint 오류 없음(기존 경고만 남음)
- 프런트엔드: Vite 프로덕션 빌드 성공

## 2026-07-13 성능 및 식단 추가 UX 개선

- 목표 영양소와 음식별 상세 영양소를 따로 계산하던 두 API를 `/api/nutrition/today/overview` 하나로 통합했다.
- 동일한 군 급식 메뉴의 식품 DB 매칭 결과를 10분간 캐시해 화면 이동 때마다 반복되는 다수의 DB 검색을 제거했다.
- 일치하지 않는 긴 메뉴명의 모든 부분 문자열을 DB에서 조회하던 구조를 상위 후보 3개로 제한해 최초 영양소 조회의 쿼리 폭증을 막았다.
- React 개발 모드의 중복 렌더링에서도 영양소 overview 요청 하나를 공유하도록 했다.
- 필수 설정 모달의 프로필·부대·기구 조회를 10초간 공유해 페이지 이동마다 발생하던 중복 요청을 줄였다.
- 식단 추가 검색의 입력 대기 시간을 400ms로 조정해 입력 도중의 불필요한 검색 호출을 줄였다.
- 음식 선택 버튼을 `선택/선택됨`으로 명확하게 바꾸고 상단 `닫기`, 하단 고정 `취소/추가하고 식단으로 돌아가기` 버튼을 추가했다.
- 저장 성공 시 추가 화면을 히스토리에서 교체하고 식단 화면을 새로 불러와 추가한 음식이 즉시 해당 끼니에 표시되도록 했다.

## 2026-07-13 군 급식 매칭 정확도 및 식품 데이터 개선

- `keyword_similarity`로 생성된 저품질 alias는 자동 매칭 DB에 넣지 않고 검수용 `excluded_aliases` 시트에 보존하도록 했다.
- 복합 메뉴는 느슨한 단일 음식 토큰 매칭보다 재료 기반 복합 추정을 우선하도록 변경했다.
- 후보 alias 자체의 문자열 유사도, 음식 품질 상태, 메뉴와 음식 카테고리·조리법 호환성을 함께 검증한다.
- 의미 토큰을 앞부분 문자열이 아닌 음식 핵심어 중심으로 선택하고 유사도 최소 기준을 강화했다.
- `농심 / 웰치제로 그레이프맛 / 355mL/캔`처럼 브랜드·제품·복합 포장 단위가 여러 줄인 항목을 하나로 파싱한다.
- 식품 재가져오기 및 수동 매칭 승인 직후 영양소 분석 캐시를 초기화한다.
- 기존 원본을 보존하면서 안전 alias, 제외 alias, 수동 군 급식 매칭, 제공량 기준, 운영 가이드를 포함한 `food_data_7_13.xlsx`를 새로 제작했다.

## 2026-07-14 7296부대 점심 재검증

- `감자짜글이` 원재료 탐색에서 출현 빈도가 높은 `감자칩` 대신 감자·전분/채소 원재료를 우선하도록 카테고리 적합도 점수를 추가했다.
- 복합 재료 후보는 열량·탄수화물·단백질·지방 값이 완전한 식품을 우선하며, 과자·스낵·음료 후보는 원재료 검색에서 감점한다.
- `크리미양파드레싱`을 드레싱 재료로 인식하고 조미식품/소스 카테고리를 우선하도록 했다.
- `food_data_7_13.xlsx`에 감자짜글이, 훈제연어스테이크&크리미양파드레싱, 초코찰떡 대표값과 7개 메뉴의 검증된 1회 제공량을 추가했다.
- 7월 14일 점심 재실행 결과는 7/7 메뉴 매칭, 원시 추정 1,084 kcal, 공식 표시 및 항목 합계 1,083 kcal이다.
- 최종 추정 영양소는 탄수화물 148.4g, 단백질 57.8g, 지방 30.2g이다.

## 2026-07-14 전체 부대 식단 이력 기반 추론 구조 개선

- 제공된 ZIP의 27개 파일을 검사해 정상 `.xls` 26개를 분석하고, 잘못된 접근 안내만 포함한 1968부대 HTML 파일 1개는 제외했다.
- 2021-01-01부터 2026-08-31까지 26개 부대의 메뉴 관측 427,038건을 정규화해 표준 메뉴 11,566개와 부대별 프로필을 만들었다.
- 알레르기 번호, `수의계약`, `수의`, `임가공`, 포장 단위, 월 표기를 제거하고 쇠고기/소고기 등 동의어를 같은 검색 키로 통합했다.
- 숫자 또는 `kcal`만 남은 오류 행 278건과 비정상 0 kcal을 추론 대상에서 제외했다.
- 평균값 하나로 단정하지 않고 중앙값·사분위수·최솟값·최댓값·표본 수·부대 수·신뢰도와 검수 사유를 보존한 `military_menu_data.xlsx`를 제작했다.
- 저장소에는 DB 적재에 필요한 `military_menu_master`, `unit_profiles`만 담은 경량 런타임 사본을 두고, 별도 전체 분석 파일에는 감사·검수 시트를 유지했다.
- `military_menu_profiles`, `military_menu_unit_profiles` DB 모델과 `importMilitaryMenus` 태스크를 추가했다.
- 런타임 매칭 순서를 `부대별 군 식단 프로필 → 전체 군 식단 프로필 → 기존 식품/복합 재료 추정`으로 변경했다.
- 공식 끼니 열량과 매칭 항목 합계가 다를 때 마지막 항목의 비율을 강제로 100%까지 올리던 오류를 수정했다.
- 분모자·마라·닭, 들깨·무나물의 재료 토큰을 추가해 군 메뉴 칼로리와 기존 식품 DB의 탄단지를 결합할 수 있게 했다.
- 7296부대 2026-07-14 저녁 예시를 재실행한 결과 6/6 메뉴가 군 식단 프로필로 매칭됐고, 공식 987 kcal에 맞춰 항목 합계도 987 kcal로 보정됐다. 탄수화물 148.2g, 단백질 26.8g, 지방 33.5g이며 6개 항목 모두 탄단지가 채워졌다.
- 원본 식단표에 재료 중량과 탄수화물·단백질·지방이 없으므로, 군 식단 프로필만으로 탄단지를 새로 생성하지 않는다. 기존 식품 DB가 안전하게 매칭되거나 재료 기반 추정이 가능한 항목만 탄단지를 보완한다.

## 2026-07-14 날짜별 군 식단 및 운영 안전성 개선

- 원본 관측 427,038건에서 유효한 `부대+날짜+끼니+메뉴` 조합 373,489개를 `military_menu_daily_profiles.csv.gz`로 생성했다.
- 영양 분석에 `MealMenu.mealDate`를 전달하고 날짜별 값이 있으면 중앙값 프로필보다 먼저 사용하도록 했다.
- 7296부대 2026-07-14 저녁은 원본 값 기준으로 밥 360, 새우살감자국 74.85, 분모자마라찜닭 309.66, 해물완자전 190, 들깨무나물 36.33, 깍두기 16 kcal를 사용한다.
- 날짜별 원본을 적용한 재실행 결과는 원시 추정과 공식 표시가 모두 987 kcal이며, 탄수화물 150.8g, 단백질 26.5g, 지방 32.5g이다.
- API에 `calorieSource/calorieConfidence`와 `macroSource/macroConfidence`를 분리해 같은 항목에서도 칼로리 관측값과 탄단지 추정값의 출처를 구분했다.
- 식단 화면에 `해당 날짜 부대 식단`, `부대 식단 중앙값`, `재료 기반 추정` 등 출처를 표시한다.
- 군 식단 엑셀과 날짜별 압축 CSV를 하나의 트랜잭션으로 적재하고, 필수 컬럼과 최소 유효 행 수를 삭제 전에 검사한다.
- 영양 분석 캐시는 최대 3,000개로 제한하고 만료 항목과 오래된 항목을 정리하도록 했다.
- 공식 칼로리 반올림 오차를 마지막 메뉴 하나에 몰지 않고 매칭된 전체 메뉴에 1 kcal 단위로 분산하도록 했다.
